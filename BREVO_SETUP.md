# Brevo (SendinBlue) API Setup - ฟรี 300 อีเมล/วัน!

## ✅ **วิธีที่ง่ายที่สุด - ใช้ Gmail ที่มีอยู่แล้ว ผ่าน Brevo API**

### ทำไมต้องใช้ Brevo?
- ✅ **ฟรี 300 อีเมล/วัน** (พอสำหรับ demo)
- ✅ ใช้ HTTP API (Railway ไม่บล็อก)
- ✅ สมัครง่าย ไม่ต้อง verify domain
- ✅ ส่งได้หาทุกคน ไม่มีข้อจำกัด

---

## 📋 **ขั้นตอนการตั้งค่า (3 นาที)**

### 1. สมัคร Brevo (ฟรี)
- ไปที่: https://www.brevo.com/
- คลิก "Sign up free"
- ใช้อีเมล `noretify32@gmail.com` หรืออีเมลอื่นก็ได้

### 2. สร้าง API Key
- เข้าสู่ระบบ Brevo
- ไปที่: **Settings (⚙️) → SMTP & API → API Keys**
- คลิก **"Generate a new API key"**
- ตั้งชื่อ: `Railway Booking System`
- คัดลอก API Key (เริ่มต้นด้วย `xkeysib-...`)

### 3. เพิ่ม Environment Variable ใน Railway
```
BREVO_API_KEY=xkeysib-xxxxxxxxxxxxxxxxxxxxx
```

### 4. Deploy!
Railway จะ auto-deploy และส่งอีเมลได้ทันที! 🚀

---

## 💡 **ข้อดี:**
- ✅ ใช้อีเมล Gmail ที่มีอยู่แล้ว (`noretify32@gmail.com`)
- ✅ ไม่ต้องสร้าง App Password ใหม่
- ✅ ฟรี 300 อีเมล/วัน (พอสำหรับ demo)
- ✅ ส่งได้หาทุกคนทันที
- ✅ ใช้งานได้ทั้ง localhost (Gmail SMTP) และ Railway (Brevo API)

---

## 🔧 **การทำงาน:**
1. **Localhost:** ใช้ Gmail SMTP (EMAIL_USER + EMAIL_PASS ที่มีอยู่)
2. **Railway:** ใช้ Brevo HTTP API (ไม่โดน SMTP block)
3. **Fallback:** ถ้าทุกอย่าง fail → สมัครสมาชิกสำเร็จต่อได้ (แค่ไม่มีอีเมล)
